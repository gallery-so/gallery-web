Create your own local env files in this directory before running the mobile app.

The private repo previously contained tracked environment files here. Those values were removed from the public prep copy.

Suggested local files:

- `.env.local`
- `.env.dev`
- `.env.prod`
